#ifndef DEF_FIXED_HPP
#define DEF_FIXED_HPP

#include <string>
#include <iostream>

class Fixed
{
    public:
        Fixed(/* args */);
        Fixed(/* args */);
        Fixed(/* args */);
        ~Fixed();

    private:
        /* data */
};

#endif